# Web-based UI dependencies (pywebview + FastAPI)
# Install with: pip install -r requirements-web.txt

# Core — opens the app window
pywebview>=4.0

# Web server — serves the HTML/CSS/JS frontend
fastapi>=0.104.0
uvicorn>=0.24.0

# Optional — for PDF summarizer (if not already installed)
# fpdf2>=2.7.0
